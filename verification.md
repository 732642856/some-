# 验证记录

日期：2026-06-20

## 已完成检查

- `Info.plist` 通过 `plutil -lint`
- `PrivacyInfo.xcprivacy` 通过 `plutil -lint`
- `project.pbxproj` 通过 `plutil -lint`
- `some.xcscheme` 通过 XML parser 校验
- Asset Catalog 的 JSON 文件通过 `python3 -m json.tool`
- App Store 图标 `AppStore/icon-1024.png` 为 1024x1024 RGB PNG
- AppIcon 集合包含 iPhone、iPad 和 iOS marketing 所需尺寸
- 上架文案未把 `flomo` 作为应用名称、关键词或宣传语使用
- 已补充 `research-and-gap-audit.md`，记录 flomo/MoeMemos/usememos 对标与后续差距
- 所有 app Swift 文件均已纳入 Xcode app target
- `SomeTests` 测试 target 已加入工程和共享 scheme
- 已补充 AI 相关单元测试：余弦相似度、AI 洞察 Prompt、归档过滤
- 已新增 AI 模块并确认所有新增 Swift 文件纳入 Xcode app target
- 已扫描 `TODO` / `FIXME` / `try!` / `as!` / 旧 OpenAI 模型名，未发现命中
- `PrivacyInfo.xcprivacy` 已按可选 AI 请求保守披露 Other User Content / App Functionality，并声明 UserDefaults required reason API
- 已按 `openai-docs` 技能安装 OpenAI Docs MCP：`openaiDeveloperDocs`。当前会话需要重启后才会暴露对应工具，本轮代码依据官方网页文档与保守默认模型实现。
- 2026-06-21：已将最新工程从 Codex 历史工作区收拢到 `/Users/wuyongnaren/Documents/some随记`
- 2026-06-21：已新增隐私锁、每日回顾提醒、链接识别与打开
- 2026-06-21：已确认 `AppLockView.swift`、`ReminderManager.swift`、`LinkExtractor.swift` 纳入 Xcode app target
- 2026-06-21：已确认 `LocalAuthentication.framework`、`UserNotifications.framework` 纳入 app target
- 2026-06-21：`Info.plist`、`PrivacyInfo.xcprivacy`、`project.pbxproj` 通过 `plutil -lint`
- 2026-06-21：`some.xcscheme` 通过 `xmllint --noout`
- 2026-06-21：已补充隐私锁默认锁定、每日提醒默认时间、链接去重解析相关单元测试
- 2026-06-21：主存储已从单 JSON 文件升级为 SQLite，旧 JSON / `.bak.json` 会在首次打开时迁移
- 2026-06-21：已确认 `SQLiteMemoDatabase.swift` 纳入 Xcode app target，`libsqlite3.tbd` 纳入 app Frameworks
- 2026-06-21：已补充 SQLite 跨实例持久化与旧 JSON 备份迁移相关单元测试
- 2026-06-21：已新增 Share Extension target，支持从其他 App 分享文本/链接到 some
- 2026-06-21：已新增 App Group entitlements，占位 group 为 `group.com.732642856.some`
- 2026-06-21：已新增共享存储路径与旧 Documents 数据迁移逻辑
- 2026-06-21：已增强快速输入：自动聚焦、保存并继续、标签建议、链接预览
- 2026-06-21：`Info.plist`、`PrivacyInfo.xcprivacy`、主 App/扩展 entitlements、Share Extension `Info.plist`、`project.pbxproj` 通过 `plutil -lint`
- 2026-06-21：已补充分享内容组合/URL 去重相关单元测试
- 2026-06-21：已新增 SQLite FTS5 搜索索引，记录新增、编辑、删除时同步索引
- 2026-06-21：已新增搜索语法解析：`#标签`、`tag:标签`、`is:pinned`、`is:unpinned`、`is:archived`、`is:active`
- 2026-06-21：已补充搜索解析、标签/置顶组合过滤、归档搜索、索引更新删除相关单元测试
- 2026-06-21：已新增 `docs/open-source-reuse-audit.md`，记录 MoeMemos/usememos 与全网开源候选的许可证、可复用范围和下一步优先级
- 2026-06-21：已新增 `MarkdownMemoTextView`，列表和详情页支持系统 Markdown 阅读渲染
- 2026-06-21：已新增 `MemoTaskParser`，支持 `- [ ]` / `- [x]` 任务项识别与详情页勾选写回
- 2026-06-21：已补充任务项解析、切换和 Store 写回相关单元测试
- 2026-06-21：已新增 `SaveMemoIntent` 与 `SomeAppShortcuts`，支持快捷指令保存新随记
- 2026-06-21：已补充 `MemoStore.addMemo` 输入清洗、空内容忽略、标签保留相关单元测试
- 2026-06-21：已新增 `SharedAttachmentStore`，支持 App Group 本地附件保存、引用解析、文件名去重
- 2026-06-21：已扩展 Share Extension 支持图片/文件分享，主 App 列表和详情页展示附件卡片
- 2026-06-21：已补充附件引用解析、链接过滤、文件名去重、任务行号映射相关单元测试
- 2026-06-21：已新增完整备份 `MemoBackupArchive`，导出会把 memo 与本地附件数据一起编码，导入时会恢复附件并重写引用
- 2026-06-21：已修复完整备份导出为文件、设置页识别 `{}` 完整备份、坏附件备份半导入、重复备份生成孤儿附件等数据安全问题
- 2026-06-21：已修复编辑/删除 memo 时误删仍被其他 memo 引用的共享附件问题
- 2026-06-21：已修复完整备份导出在附件文件缺失时静默降级为不完整备份的问题，现在会明确失败并提示用户
- 2026-06-21：已修复快速输入保存失败时清空草稿的问题，现在会保留草稿并提示用户
- 2026-06-21：已修复快捷指令保存失败时误报“空内容”的问题，现在会区分空内容与存储失败
- 2026-06-21：已修复 AI 洞察保存失败时误报“已保存”的问题，现在会按真实写入结果反馈
- 2026-06-21：已将主 App Bundle ID、Share Extension Bundle ID、App Group 抽成 build settings / CI secrets，减少发布签名时多处手改不一致的风险
- 2026-06-21：已更新 TestFlight workflow，支持主 App 和 Share Extension 两套 App Store provisioning profile
- 2026-06-21：已修正 README 附件删除说明，并补充 TestFlight 真机验收清单
- 2026-06-21：已增强 TestFlight workflow 的 macOS 兼容性与失败提示：使用 `base64 -D`、缺少 secrets 时快速失败、设置 keychain partition list、找不到 IPA 时直接报错
- 2026-06-21：已增强 App Group 运行时读取逻辑，`APP_GROUP_IDENTIFIER` 为空字符串时会回退到默认占位值，避免空 group 导致主 App / Share Extension 路径异常
- 2026-06-21：已修正隐私政策中“默认本地保存”和“可选 AI 会发送所需内容”的表述，避免 App Store 审核口径前后冲突
- 2026-06-21：已新增 `.gitignore`，避免 Xcode 本地状态、构建产物、Archive、IPA 和签名材料误提交
- 2026-06-21：已修正文档中的旧工作区路径，并把隐私政策联系方式占位改为 App Store 支持链接口径
- 2026-06-22：已复查当前主仓库、旧 Codex 工作区和已克隆开源参考，未发现当前主仓库外仍需合入的新 some 工程碎片。
- 2026-06-22：已对照 flomo 101 官方功能导航、MoeMemos 与 usememos/memos，确认本轮优先补搜索二期而非继续扩展表面功能。
- 2026-06-22：已新增最近搜索、保存搜索、搜索结果数量和卡片命中摘录。
- 2026-06-22：已补充最近搜索去重/持久化、最近搜索上限、保存搜索持久化/应用、命中摘录相关单元测试。
- 2026-06-22：`Info.plist`、`PrivacyInfo.xcprivacy`、主 App/扩展 entitlements、Share Extension `Info.plist`、`project.pbxproj` 通过 `plutil -lint`。
- 2026-06-22：`some.xcscheme` 通过 `xmllint --noout`。
- 2026-06-22：已扫描 `text.magnifyingglass`、`TODO`、`FIXME`、`try!`、`as!`，未发现命中。
- 2026-06-22：已对照 usememos `MemoRelation` / `REFERENCE` / `COMMENT` 与 flomo 101 引用批注，新增卡片引用 v1。
- 2026-06-22：已新增 `MemoReferenceParser`，并确认纳入主 App 与 Share Extension sources。
- 2026-06-22：已补充内部引用解析、隐藏引用行、添加引用、反向引用、引用去重、普通链接过滤内部引用相关单元测试。
- 2026-06-22：已复查主仓库、未跟踪文件和 Documents 下同名目录，未发现当前主仓库外的活跃 some 工程碎片；外部只看到旧 Codex 会话目录。
- 2026-06-22：已对照 usememos/memos 的 memo relations、link metadata 与内容 payload/filter 思路，新增搜索三期第一段：`has:link`、`has:attachment`、`has:task`、`has:open-task`、`has:completed-task`、`has:reference`、`has:backlink`，并支持 `-has:` / `no:` / `without:` 排除。
- 2026-06-22：已补充结构化搜索解析、未知 `has:` 回落为普通文本、链接/附件/任务/引用/反向引用筛选、排除筛选相关单元测试。
- 2026-06-22：已将 `LinkExtractor.swift` 纳入 Share Extension sources，避免共享 `MemoStore` 使用链接筛选时扩展 target 缺符号。
- 2026-06-22：本轮 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧版 `xcrun swiftc -parse` 已覆盖新增搜索解析器、`MemoStore` 和测试文件的语法层检查。
- 2026-06-22：再次复查主仓库、未跟踪文件和 Documents 下同名目录，未发现当前主仓库外的活跃 some 工程碎片。
- 2026-06-22：已对照 Joplin 搜索文档的 `created:` / `updated:` 日期过滤和错误过滤器回落原则，新增 `created:` / `updated:` 日期筛选，支持年/月/日范围和 `>` / `>=` / `<` / `<=` 边界过滤。
- 2026-06-22：已补充日期筛选解析、无效日期回落普通文本、创建日期范围筛选、更新时间筛选与普通文本组合相关单元测试。
- 2026-06-22：根据用户修订，已确认 some 的目标不再只是 flomo 式轻备忘，而是本地优先个人素材库，覆盖电子手帐、工作日志、网页摘录、图片编辑和电子衣橱。
- 2026-06-22：已补做本地碎片扫描，新增发现并记录 `/Users/wuyongnaren/Documents/Codex/2026-06-20/wo/outputs/some`；与当前主仓库比对后未发现需要补回的独有新功能文件。
- 2026-06-22：已把每轮开发前强制审计流程写入 `research-and-gap-audit.md`：Git 状态、未跟踪文件、全量文件清单、旧窗口目录、Xcode target membership、全网开源检索和许可证记录。
- 2026-06-22：已重新检索并记录新目标相关开源候选：图片裁剪/编辑 `Mantis`、`TOCropViewController`、`HXPhotoPicker`，网页提取 `ReadabilityKit`，OCR `OpenFind` / `SwiftOCRKit`，衣橱参考 `AURA` 等。
- 2026-06-22：已明确图片清理边界：支持用户拥有或获授权图片的修复、对象清理、背景处理和排版，不把移除第三方版权标识、平台水印或规避授权限制作为产品目标。
- 2026-06-22：已更新下一步优先级：先做统一素材模型、多模态采集、网页/OCR 摘录、电子衣橱、电子手帐和图片编辑 MVP，再回补 flomo 小组件、禅定模式、MCP/API、AI 语音和记忆档案等缺口。
- 2026-06-22：已按用户提供的 `/Users/wuyongnaren/Downloads/辰星话本.JPG` 替换 some AppIcon 和 App Store 1024 图标，并把 UI 色系调整为浅粉、雾蓝、淡紫、柔白的小清新方向。
- 2026-06-22：本轮视觉更新前已复查 Git 状态、未跟踪文件、Documents 下 some 相关目录，并做开源检索；图标生成使用用户提供图片和本地 Pillow，不引入第三方运行时代码。
- 2026-06-22：已完成统一素材模型 v1，新增 `MemoAsset` 与 SQLite `memo_assets` 表，保存、导入、更新、删除 memo 时会同步维护文本/链接/附件/任务/引用素材索引。
- 2026-06-22：已补充素材索引单元测试：文本/链接/附件/任务/引用解析、memo 更新同步删除旧链接素材、memo 删除清空素材索引。
- 2026-06-22：本轮素材模型开发前已复查 Git 状态、未跟踪文件、Documents 下 some 相关目录，并检索 GRDB、Swift/iOS 媒体附件模型和衣橱模型；未找到可直接复制的成熟 Swift 素材库模型，暂不引入新依赖。
- 2026-06-22：本轮多模态采集入口开发前再次复查 Git 状态、未跟踪文件、当前文件清单和 Documents 下 some 相关旧目录；GitHub/API 检索 `SwiftUI PhotosPicker sample`、`SwiftUI fileImporter sample`、`SwiftUI journal photo attachments`、`SwiftUI memo attachment app` 等关键词，未找到许可证清晰且可直接复制的成熟模块。
- 2026-06-22：已用 Apple `PhotosUI.PhotosPicker` 和 SwiftUI `.fileImporter` 实现快速导入入口 v1；相册图片/视频和文件会保存为本地附件 memo，并进入 `MemoAsset` 索引。
- 2026-06-22：已新增首页“素材”模式，按素材类型筛选文本、链接、附件、任务、引用等资产；图片附件显示本地缩略图，并可跳回来源记录。
- 2026-06-22：已补充 `addAttachmentMemo` 单元测试，覆盖附件导入 memo、附件引用解析和素材索引字段。
- 2026-06-22：本轮 `git diff --check`、`plutil -lint some/Info.plist some.xcodeproj/project.pbxproj` 通过；`xcrun swiftc -parse` 已覆盖 `Memo.swift`、`MemoStore.swift`、`ContentView.swift` 和 `SomeTests.swift`。`QuickCaptureView.swift` 因 PhotosPicker 数据加载使用 Swift concurrency，旧 Swift 5.4 parser 仍无法解析 `await`，需要 Xcode 16 完整验证。
- 2026-06-22：本轮继续前已再次执行 Git 状态、未跟踪文件、全量文件清单、Documents/Codex 旧 some 目录搜索和 `diff -qr` 比对；当前主仓库仍是旧工作区功能超集，未发现需要补回的外部碎片。
- 2026-06-22：已重新检索网页摘录/OCR相关开源项目并记录到 `docs/open-source-reuse-audit.md`：`ReadabilityKit`、`SwiftSoup`、`OpenFind`、`SwiftOCRKit` 均未直接复制；本轮采用 Apple `URLSession` / `LinkPresentation` 实现网页摘录 MVP。
- 2026-06-22：已新增网页摘录格式解析、`addWebClip` 保存、`webClip` 素材索引和 `has:web` / `has:webclip` 搜索筛选；素材库网页条目可直接打开原链接。
- 2026-06-22：已补充网页摘录解析、格式生成、网页素材索引、网页搜索筛选和 `addWebClip` 单元测试。
- 2026-06-22：本轮 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧版 `xcrun swiftc -parse` 已覆盖 `LinkExtractor.swift`、`MemoSearchQuery.swift`、`Memo.swift`、`MemoStore.swift`、`ContentView.swift` 和 `SomeTests.swift`。`QuickCaptureView.swift` 仍因本机 Swift 5.4 不支持 async/await 无法解析，需要 Xcode 16 做完整编译和 XCTest。
- 2026-06-23：已新增网页摘录 v2，`WebClipExtractor` 负责本地 HTML 清洗、正文段落抽取、噪音过滤、实体解码和段落评分，快速输入保存时写入来源与摘录卡行。
- 2026-06-23：网页摘录 v2 已补充正文清洗、去重、噪音过滤、摘要回退和摘录卡格式测试；本轮 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过，旧版 `xcrun swiftc -parse` 覆盖 `LinkExtractor.swift`、`WebClipExtractor.swift`、`Memo.swift`、`MemoStore.swift` 和 `SomeTests.swift`。`QuickCaptureView.swift` 仍因本机 Swift 5.4 不支持 async/await 无法完整解析，需要 Xcode 16 做完整编译和 XCTest。
- 2026-06-23：已新增网页/OCR 统一摘录片段 v1，`ClipFragmentExtractor` 可从网页摘要/重点和 OCR 行生成统一候选，快速输入网页摘录会展示片段选择卡，保存时只写选中片段。
- 2026-06-23：摘录片段阶段已补充网页+OCR 片段抽取、合并文本生成和 OCR 行去重测试；本轮 `git diff --check`、`plutil -lint` 通过，旧版 `xcrun swiftc -parse` 覆盖 `LinkExtractor.swift`、`WebClipExtractor.swift`、`ClipFragmentExtractor.swift`、`Memo.swift` 和 `SomeTests.swift`。`ImageTextRecognizer.swift` / `QuickCaptureView.swift` 仍因本机 Swift 5.4 不支持 async/await 无法完整解析，需要 Xcode 16 做完整编译和 XCTest。
- 2026-06-22：已新增图片/截图 OCR v1，导入图片后使用 Apple Vision `VNRecognizeTextRequest` 在本机识别文字，识别成功会保存为“图片文字” memo 并保留原附件引用。
- 2026-06-22：已新增 `ImageTextRecognizer.swift`，并确认纳入主 App target；OCR 记录会生成 `screenshot` 素材，搜索支持 `has:ocr` / `has:screenshot` / `has:图片文字`。
- 2026-06-22：已补充 OCR memo 生成、去重、图片文字素材索引、`has:ocr` 搜索和避免重复附件引用相关单元测试。
- 2026-06-22：本轮 `git diff --check`、`plutil -lint some.xcodeproj/project.pbxproj some/Info.plist some/PrivacyInfo.xcprivacy SomeShareExtension/Info.plist`、`xmllint --noout some.xcodeproj/xcshareddata/xcschemes/some.xcscheme` 通过；旧版 `xcrun swiftc -parse` 已覆盖 `LinkExtractor.swift`、`MemoSearchQuery.swift`、`Memo.swift`、`MemoStore.swift` 和 `SomeTests.swift`。
- 2026-06-22：已新增拍照导入 v1：快速输入工具栏相机按钮使用系统 `UIImagePickerController` 拍摄单张图片，保存为本地 JPEG 附件，并复用现有 OCR、附件 memo、素材索引和备份链路；`Info.plist` 已补充 `NSCameraUsageDescription`。
- 2026-06-22：已新增录音入口 v1：快速输入工具栏麦克风按钮使用 Apple `AVAudioRecorder` / `AVAudioSession` 录制本地 m4a 音频，停止后保存为附件 memo，并进入 `audio` 素材索引；搜索支持 `has:audio` / `has:录音` / `has:音频`。
- 2026-06-22：已补充录音附件生成 audio 素材测试，并在 `Info.plist` 补充 `NSMicrophoneUsageDescription`。
- 2026-06-22：已新增拍视频导入 v1：快速输入工具栏视频按钮复用系统 `UIImagePickerController` 的 `.video` 模式，保存系统相机返回的 movie URL 为本地附件 memo，并进入 `video` 素材索引；搜索支持 `has:video` / `has:视频` / `has:录像`。
- 2026-06-22：视频拍摄入口开发前已重新检索 GitHub 和 Apple 官方文档；未找到适合直接复制的 iOS SwiftUI MIT 视频拍摄模块，本轮未新增第三方依赖。
- 2026-06-22：已补充附件预览的音频/视频类型判断，记录列表和详情附件区不再把音视频都显示成普通文件；已补充 `SharedAttachment` 媒体类型标记测试、视频附件生成 video 素材测试和 `has:video` 搜索筛选测试。
- 2026-06-22：本轮 `git diff --check`、`plutil -lint some.xcodeproj/project.pbxproj some/Info.plist some/PrivacyInfo.xcprivacy SomeShareExtension/Info.plist`、`xmllint --noout some.xcodeproj/xcshareddata/xcschemes/some.xcscheme` 通过；旧版 `xcrun swiftc -parse` 已覆盖 `LinkExtractor.swift`、`MemoSearchQuery.swift`、`SharedAttachmentStore.swift`、`Memo.swift`、`MemoStore.swift`、`AttachmentPreviewList.swift` 和 `SomeTests.swift`。
- 2026-06-22：已新增手帐图层底座：`ScrapbookPageLayout` / `ScrapbookLayer` 会随手帐页面写入正文内可迁移 JSON，覆盖图片、文字、贴纸、边框、背景色、位置、尺寸、旋转、字体和颜色等字段；手帐列表新增页面预览。
- 2026-06-22：本轮手帐画布开发前已重新复查 Git 状态、未跟踪文件和手帐现有边界，并通过 GitHub API 检索 SwiftUI/Swift 贴纸、拼贴、图层画布候选；未找到可直接复制的成熟 SwiftUI MIT 手帐画布，本轮未新增第三方依赖。
- 2026-06-22：已修复工程引用但 Git 未包含 `AudioTranscriber.swift` 的 P0 构建风险；语音转写工具现已纳入 App target，`Info.plist` 补充 `NSSpeechRecognitionUsageDescription`，详情页可对音频附件发起 Apple Speech 本机转写并追加到原 memo。
- 2026-06-22：本轮继续时复查 Git 状态、文件清单、未跟踪文件、语音/音频相关代码和开源检索结果；GitHub API 检索 `iOS SFSpeechRecognizer audio file transcription Swift MIT`、`SFSpeechURLRecognitionRequest Swift MIT` 均返回 0，Apple `SFSpeechRecognizer` / `SFSpeechURLRecognitionRequest` 官方文档可达，本轮未复制第三方源码。
- 2026-06-22：已将语音转写强制为 `requiresOnDeviceRecognition`，设备或语言不支持本机识别时提示不可用；同时修复异步转写完成时基于最新 memo 文本追加，避免覆盖转写期间的其他编辑。
- 2026-06-22：已新增视频缩略图 v1，`VideoThumbnailGenerator` 使用 Apple `AVAssetImageGenerator` 本机取帧；详情附件列表和素材库视频条目共用 `VideoThumbnailPreview`，后台生成预览并在失败时回退视频图标。
- 2026-06-22：本轮视频缩略图继续前已复查 Git 状态、未跟踪文件、当前文件清单、附件预览和素材库现有代码；GitHub/API 与网页检索未找到可直接复制的成熟 MIT SwiftUI 视频缩略图库，本轮未新增第三方依赖。
- 2026-06-22：已补充视频缩略图缺失文件返回 nil 的单元测试；`some/Utilities/VideoThumbnailGenerator.swift` 已纳入主 App target。
- 2026-06-22：已完成工作日志 v1，首页新增“日志”模式，可勾选已有记录，提取已完成/未完成任务，保存带来源引用的结构化工作日志；素材索引新增 `workLog`，搜索支持 `has:worklog` / `has:工作日志` / `has:日报` / `has:周报`。
- 2026-06-22：工作日志继续前已复查 Git 状态、未跟踪文件、当前文件清单和半成品差异；GitHub API 检索工作日志 SwiftUI/MIT 候选均返回 0，本轮未复制第三方源码。
- 2026-06-22：已补充工作日志结构化保存、来源引用、`workLog` 素材索引和 `has:worklog` 搜索筛选相关单元测试。
- 2026-06-22：本轮 `git diff --check`、`plutil -lint some.xcodeproj/project.pbxproj some/Info.plist some/PrivacyInfo.xcprivacy SomeShareExtension/Info.plist`、`xmllint --noout some.xcodeproj/xcshareddata/xcschemes/some.xcscheme` 通过；旧版 `xcrun swiftc -parse` 已覆盖 `VideoThumbnailGenerator.swift`、`AttachmentPreviewList.swift`、`ContentView.swift`、`Memo.swift`、`MemoStore.swift`、`MemoSearchQuery.swift` 和 `SomeTests.swift`。
- 2026-06-22：已新增手帐画布编辑 v1，手帐详情页会显示可交互画布，支持选中图层、拖拽位置、双指缩放、旋转，并把更新后的 `ScrapbookPageLayout` JSON 保存回原 memo。
- 2026-06-22：手帐画布 v1 开工前已复查 Git 状态、全量文件清单和现有手帐源码；GitHub API 检索 SwiftUI/Swift 贴纸、拖拽缩放旋转、拼贴编辑器候选均未找到可直接复制的成熟 SwiftUI/MIT 模块，本轮未复制第三方源码。
- 2026-06-22：已补充 `ScrapbookPageLayout.replacingLayout` 单元测试，覆盖替换已有图层 JSON 和给旧手帐追加图层 JSON。
- 2026-06-22：已新增手帐画布编辑 v1，详情页读取 `ScrapbookPageLayout` 后可拖拽、缩放、旋转图片/文字/贴纸/边框/形状图层，并通过 `ScrapbookPageLayout.replacingLayout` 保存回原 memo 的可迁移 JSON。
- 2026-06-22：阶段 9 开工前已复查 Git 状态、未跟踪文件、文件清单、手帐模型/视图/测试边界；GitHub API 检索 SwiftUI/Swift 拖拽缩放旋转贴纸和拼贴编辑器未找到可直接复制的成熟 MIT SwiftUI 模块，本轮未新增第三方依赖。
- 2026-06-22：已补充手帐图层 JSON 替换与缺失时追加的单元测试；本轮 `git diff --check`、`plutil -lint` 通过，旧版 `xcrun swiftc -parse` 已覆盖 `Memo.swift`、`SharedAttachmentStore.swift`、`MemoSearchQuery.swift`、`MemoTaskParser.swift`、`MemoReferenceParser.swift`、`LinkExtractor.swift` 和 `SomeTests.swift`。
- 2026-06-22：已新增图片编辑 v1，素材库图片附件可进入编辑器，支持预设比例裁剪、Core Image 滤镜、边框、文字和贴纸；保存会生成新的本地 PNG 附件，并写入带 `ImageEditRecipe` 的“图片编辑”记录。
- 2026-06-22：图片编辑阶段开工前已复查 Git 状态、全量文件清单、图片/附件/素材索引边界；GitHub API 确认 `Mantis` 与 `TOCropViewController` 为 MIT 且适合作为后续自由裁剪候选，本轮未新增第三方依赖。
- 2026-06-22：已补充图片编辑配方编解码、渲染保存、`imageEdit` 素材索引和 `has:image-edit` 搜索筛选测试；本轮 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过，旧版 `xcrun swiftc -parse` 已覆盖新增图片编辑模型、渲染器、编辑视图和测试文件。
- 2026-06-23：已新增图片编辑 v2，`ImageEditRecipe` 向后兼容新增裁剪中心/缩放微调和授权清理贴片；渲染器会在滤镜前应用可调裁剪和本地模糊贴片清理，编辑器可添加最多 6 处清理点。
- 2026-06-23：阶段 12 开工前已复查 Git 状态、全量文件清单、图片编辑模型/渲染器/UI/测试边界；GitHub API 复查 `Mantis` / `TOCropViewController` 并检索对象移除候选，未复制第三方源码、未新增依赖。
- 2026-06-23：图片编辑 v2 本轮 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧版 `xcrun swiftc -parse` 覆盖主 App 图片编辑路径，并用 `-D SOME_SHARE_EXTENSION` 覆盖分享扩展可解析路径。
- 2026-06-23：图片编辑 v3 已新增背景柔化/纯色画布底座，配方向后兼容旧 JSON；本轮 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过，旧版 `xcrun swiftc -parse` 覆盖 `Memo.swift`、`ImageEditRenderer.swift`、`ImageEditorView.swift`、`MemoStore.swift` 和 `SomeTests.swift`。
- 2026-06-23：已新增手帐图片素材追加与 PNG 导出；编辑器可选择现有图片素材加入手帐图层，并可把当前画布渲染为 PNG 附件。
- 2026-06-23：阶段 13 开工前已复查 Git 状态、未提交文件碎片、手帐编辑器、素材引用解析和相关测试；GitHub API 检索未找到可直接复制的 MIT SwiftUI 手帐/拼贴导出模块。
- 2026-06-23：手帐导出本轮 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧版 `xcrun swiftc -parse` 覆盖 `AttachmentReferenceResolver.swift`、`ScrapbookRenderer.swift`、`ScrapbookEditorView.swift`、`ContentView.swift`、`MemoStore.swift` 和相关测试。
- 2026-06-23：已新增媒体元数据与视频缩略图缓存 v1，详情附件区和素材库媒体条目可展示图片尺寸、音视频时长/分辨率与文件大小摘要，视频缩略图写入本地 `ThumbnailCache` 并随文件大小/修改时间失效。
- 2026-06-23：阶段 14 本轮 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧版 `xcrun swiftc -parse` 覆盖 `MediaMetadataExtractor.swift`、`VideoThumbnailGenerator.swift`、`AttachmentPreviewList.swift`、`ContentView.swift` 和 `SomeTests.swift`，并用 `-D SOME_SHARE_EXTENSION` 覆盖分享扩展可解析路径。
- 2026-06-22：已新增衣橱洞察 v1，基于现有 `MemoAsset` 计算分类、颜色、季节、场景、未搭配单品、常用单品和搭配建议；衣橱页可将建议填入穿搭草稿，穿搭表单已补季节字段。
- 2026-06-22：阶段 11 开工前已复查 Git 状态、全量文件清单、衣橱/穿搭模型与 UI；GitHub API 检索未找到可直接复制的现代 SwiftUI MIT 衣橱模块，旧 Swift/ObjC 项目和 Kotlin `AURA` 仅做产品参考。
- 2026-06-22：已补充衣橱洞察统计与建议单元测试；本轮 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过，旧版 `xcrun swiftc -parse` 已覆盖 `WardrobeInsightEngine.swift`、`ContentView.swift` 和 `SomeTests.swift`。

## 未能在当前环境完成

- 当前机器的 `xcode-select` 指向 Command Line Tools，且 `/Applications/Xcode.app` 是 Xcode 13.2.1；当前 iOS 16+ 工程需要 Xcode 16 或更新版本才能可靠编译、测试、模拟器运行、Archive、签名或上传 App Store Connect。
- 当前 Command Line Tools 的 Swift 编译器为旧版 Swift 5.4，无法 typecheck Swift concurrency 代码；本轮只完成了 plist、scheme、target 引用和源码静态扫描。
- 2026-06-22 复查：`xcodebuild -version` 仍失败，提示 active developer directory 为 `/Library/Developer/CommandLineTools`；`xcrun swiftc -version` 为 Apple Swift 5.4。
- 2026-06-22：`xcrun swiftc -parse some/Utilities/ImageTextRecognizer.swift` 与 `xcrun swiftc -parse some/Views/QuickCaptureView.swift` 均因 Swift 5.4 不支持当前代码中的 async/await 报错；需要 Xcode 16 或更新版本做完整编译、typecheck 和 XCTest。
- 2026-06-22：拍照导入需要真机或带可用相机的运行环境验证；当前机器无法打开模拟器/真机相机，也无法用旧 CLT 完整编译 SwiftUI + UIKit bridge。
- 2026-06-22：录音入口需要真机或可授权麦克风的模拟器环境验证；当前机器无法用旧 CLT 完整编译 SwiftUI + AVFoundation 组合，也无法完成真实录音权限弹窗测试。
- 2026-06-22：拍视频导入需要真机或可用相机/麦克风的模拟器环境验证；当前机器无法完成真实系统相机录制、权限弹窗和 movie URL 回调验证。
- 2026-06-22：语音转写需要真机或支持 Speech 的模拟器环境验证授权弹窗、本机识别可用性、不同语言和长音频表现；当前旧 CLT 只能做语法层和 plist/scheme 校验。
- 2026-06-22：视频缩略图需要在完整 Xcode 16 + 模拟器/真机环境验证真实 `.mov` / `.mp4` 文件取帧、旋转方向和长列表滚动表现；当前环境只能做静态校验和失败路径测试。
- 2026-06-23：媒体元数据与缩略图缓存仍需要在完整 Xcode 16 + 模拟器/真机环境验证真实 `.mov` / `.mp4` / `.m4a` 元数据读取、缓存目录增长、长列表滚动和大文件内存表现；当前环境已覆盖格式化、图片尺寸读取、缓存 key 失效、缓存删除和语法层检查。
- 2026-06-23：GitHub Actions run `27971176783` 显示 Build for simulator 通过、Run tests 失败；公开 annotation 暴露失败测试集中在视频附件、手帐导出、图片编辑渲染和媒体元数据。本轮已修复测试数据真实文件、图片 renderer scale、媒体 byteCount fallback 和缺失视频文件取帧边界；等待下一次远端 CI 验证。
- 2026-06-23：网页/OCR 摘录片段本轮已完成旧 Swift parser 语法验证、`git diff --check`、`plutil -lint` 与 `xmllint --noout`；新增测试覆盖网页+OCR 混合片段、摘录片段合并文本、OCR 去重行提取，以及空行分隔的多截图 OCR 块。
- 2026-06-23：GitHub Actions run `27972559437` 公开 annotation 显示 Build for simulator 因 `ClipFragmentExtractor.swift` 的 Optional `map` 类型错误失败；后续修复提交 `10761c4` 后，run `27972990477` 和最新 run `27973215557` 均已通过 Build for simulator 并进入 Run tests。当前额外补充网页/OCR 片段保存语义测试：OCR 片段不再混入网页 highlights，而是进入统一“摘录片段”块。
- 2026-06-23：穿着记录/成本洞察本轮通过 `git diff --check`、`plutil -lint`、`xmllint --noout`、旧 Swift parser 覆盖 `DateFormatters.swift`、`MemoSearchQuery.swift`、`ClipFragmentExtractor.swift`、`WardrobeInsightEngine.swift`、`Memo.swift`、`MemoStore.swift`、`ContentView.swift` 和 `SomeTests.swift`；测试新增覆盖穿着记录素材、搜索、成本/次、最近穿着和旧穿搭次数回归。
- 2026-06-23：洗护记录与旅行打包本轮新增素材索引、搜索筛选、结构化保存和衣橱页表单；本地 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过，旧 Swift parser 覆盖 `Memo.swift`、`MemoSearchQuery.swift`、`MemoStore.swift`、`ContentView.swift` 和 `SomeTests.swift`。
- 2026-06-23：手帐样式增强本轮新增 `ScrapbookStyleCatalog`、画布底色、字体/贴纸/花边预设、文字色/底色/边框色、字号/圆角/线宽编辑，并让编辑器、列表缩略图和 PNG renderer 使用同一套字体风格。新增测试覆盖样式目录匹配、预设应用、默认手帐胶片框匹配和带样式图层 PNG 渲染；本地 `git diff --check`、`plutil -lint`、`xmllint --noout`、`.github/workflows/ios-ci.yml` YAML 解析通过，旧 Swift parser 覆盖 `Memo.swift`、`ScrapbookRenderer.swift`、`ScrapbookEditorView.swift`。
- 2026-06-23：GitHub Actions run `27975351182` 已确认 Build for simulator 通过、Run tests 因 `appintentsnltrainingprocessor` 解析 `extract.actionsdata` 失败而退出 65；本轮在 CI test 构建中加入 `CI_DISABLE_APP_INTENTS` 与 `EXCLUDED_SOURCE_FILE_NAMES=SaveMemoIntent.swift SomeAppShortcuts.swift`，正式 App build 仍保留 App Intents。待推送后用新 run 复验。
- 2026-06-23：GitHub Actions run `27976760975` 复验显示 Build for simulator 通过、Run tests 仍因 AppIntents metadata/NL training processor 退出 65。已进一步隔离 CI 派生数据：正式 build 使用 `${RUNNER_TEMP}/DerivedData-build`，测试构建使用 `${RUNNER_TEMP}/DerivedData-test` 和 `clean test`，避免测试阶段复用正常构建生成的 AppIntents `extract.actionsdata`。待推送后用新 run 复验。
- 2026-06-23：GitHub Actions run `27978220498` 复验显示 Build for simulator 通过、Run tests 仍退出 65；公开 annotation 显示 AppIntents metadata processor 在测试构建中处理 `ZIPFoundation` 与 `SomeShareExtension`，并涉及 `ExtractedAppShortcutsMetadata.stringsdata`。本轮继续收紧 CI test 构建，增加 `SWIFT_EMIT_LOC_STRINGS=NO`，避免测试阶段生成 Swift 本地化/App Shortcuts metadata；正式 build 不使用该关闭项。
- 2026-06-23：手帐 PDF 导出本轮通过 `git diff --check`、`plutil -lint`、`xmllint --noout`、`.github/workflows/ios-ci.yml` YAML 解析，旧 Swift parser 覆盖 `ScrapbookRenderer.swift`、`MemoStore.swift`、`ScrapbookEditorView.swift` 和 `SomeTests.swift`。新增测试覆盖 PDF 数据头、PDF 附件保存、PDF 引用写回和素材索引。
- 2026-06-23：衣橱天气推荐/自动打包/洗护提醒本轮通过 `git diff --check`、`plutil -lint`、`xmllint --noout`，旧 Swift parser 覆盖 `DateFormatters.swift`、`Memo.swift`、`WardrobeInsightEngine.swift`、`MemoStore.swift`、`ContentView.swift` 和 `SomeTests.swift`。新增测试覆盖天气穿搭、待洗/送洗单品避让、洗护提醒和打包建议。
- 2026-06-23：图片编辑版式模板/导出预设本轮通过 `git diff --check`，旧 Swift parser 覆盖 `Memo.swift`、`ImageEditRenderer.swift`、`ImageEditorView.swift`、`MemoStore.swift` 和 `SomeTests.swift`。新增测试覆盖 `layoutPreset` 编解码、旧 JSON 兼容、模板默认值、保存正文模板字段和输出文件名。
- 2026-06-23：多图拼贴 MVP 本轮通过 `git diff --check`、`plutil -lint`、`xmllint --noout`，旧 Swift parser 覆盖 `Memo.swift`、`ScrapbookRenderer.swift`、`MemoStore.swift`、`ContentView.swift` 和 `SomeTests.swift`，并用 `-D SOME_SHARE_EXTENSION` 覆盖分享扩展路径。新增测试覆盖拼贴布局、PNG 附件生成、原图引用和素材索引。
- 2026-06-23：远端 GitHub Actions 最新状态查询被审批服务 503 拦截，阶段 17 暂不能继续读取公开 run 详情。本轮先推进摘录片段素材索引：新增 `clipFragment` 素材类型、`has:clip` / `has:摘录片段` 搜索筛选、素材库摘录图标和片段块解析测试。本地 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧 Swift parser 覆盖 `ClipFragmentExtractor.swift`、`LinkExtractor.swift`、`MemoReferenceParser.swift`、`MemoTaskParser.swift`、`SharedAttachmentStore.swift`、`MemoSearchQuery.swift`、`Memo.swift`、`SomeTests.swift`，并用 `-D SOME_SHARE_EXTENSION` 覆盖分享扩展 `MemoStore.swift` 路径。
- 2026-06-23：多链接批量网页摘录阶段新增 `MemoStore.addWebClip(_:, selectedFragments:)` 与 QuickCapture 多 URL 批量保存入口；测试覆盖选中片段保存、摘录片段素材索引和多个网页摘录分别保存。`ClipFragmentExtractor.swift` 已补入 Share Extension Sources，避免分享扩展编译 `MemoAsset.assets` 时缺符号。本地 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧 Swift parser 覆盖 `WebClipExtractor.swift`、`LinkExtractor.swift`、`ClipFragmentExtractor.swift`、`MemoReferenceParser.swift`、`MemoTaskParser.swift`、`SharedAttachmentStore.swift`、`MemoSearchQuery.swift`、`Memo.swift`、`MemoStore.swift`、`SomeTests.swift`，并用 `-D SOME_SHARE_EXTENSION` 覆盖分享扩展路径。`QuickCaptureView.swift` 因本机 Swift 5.4 不支持既有 async/await 与新式 `if let` 语法仍无法单文件 parse，需要 Xcode 16 / CI 完整验证。
- 2026-06-23：工作日志项目/日期/模板增强已新增项目、日期范围和模板字段，并在 UI 提供日报、周报、项目汇报、复盘模板快捷填充。本地 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧 Swift parser 覆盖 `DateFormatters.swift`、`LinkExtractor.swift`、`ClipFragmentExtractor.swift`、`MemoReferenceParser.swift`、`MemoTaskParser.swift`、`SharedAttachmentStore.swift`、`MemoSearchQuery.swift`、`Memo.swift`、`MemoStore.swift`、`SomeTests.swift`，并用 `-D SOME_SHARE_EXTENSION` 覆盖分享扩展路径。
- 2026-06-23：截图/OCR 区域识别底座新增 `ImageTextRegion`、区域裁图识别入口和区域 OCR memo 文本，并修复区域/附件元数据进入 OCR 素材摘要的问题。本地 `git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧 Swift parser 覆盖 `ImageTextRecognizer.swift`、`Memo.swift`、`SharedAttachmentStore.swift`、`AttachmentReferenceResolver.swift`、`LinkExtractor.swift`、`ClipFragmentExtractor.swift`、`MemoReferenceParser.swift`、`MemoTaskParser.swift`、`MemoSearchQuery.swift`、`SomeTests.swift`，并用 `-D SOME_SHARE_EXTENSION` 覆盖分享扩展路径。
- 2026-06-23：GitHub Actions run `28003903915` 已确认最新 HEAD 的 Build for simulator 通过，Run tests 仍在进行中；等待期间推进媒体缩略图缓存维护。
- 2026-06-23：媒体缩略图缓存维护新增 `preheatCache(for:)` 和 `pruneCache(keeping:)`，覆盖重复 URL 去重、缺失文件失败统计、保留仍引用缓存和删除孤儿缓存。本地 `git diff --check` 通过；旧 Swift parser 覆盖 `VideoThumbnailGenerator.swift`、`SharedMemoStorage.swift`、`SomeTests.swift`。
- 2026-06-23：截图/OCR 框选 UI 已在详情页图片附件区提供“框选识别”，支持拖动选区和右下角缩放，OCR 结果追加回当前 memo 且不重复附件引用。`git diff --check`、`plutil -lint`、`xmllint --noout` 通过；旧 Swift parser 覆盖 OCR/模型/缓存和测试核心文件。包含 `MemoDetailView.swift` 的完整 UI 编译需以 GitHub Actions/Xcode 16 为准。
- 2026-06-23：衣橱洗护本地通知新增 `ReminderManager.scheduleWardrobeCareReminder`、稳定通知 identifier 和衣橱洞察安排入口；图片编辑新增人物抠图配方、Vision 分割渲染和编辑页入口；GitHub Actions Run tests 阶段新增 CI-only Share Extension 解耦。验证通过：`ruby -e "require 'yaml'; YAML.load_file('.github/workflows/ios-ci.yml')"`、`plutil -lint some.xcodeproj/project.pbxproj`、旧 Swift parser 覆盖 `Memo.swift` / `ImageEditRenderer.swift` / `ImageEditorView.swift`。`ReminderManager.swift` 单独解析被本机旧 Swift 5.4 parser 的既有 `try await` / `if let error` 语法限制阻止；完整编译以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-23：图片编辑智能主体抠图新增 `SubjectExtraction.Mode.object`，iOS 17+ 通过 `VNGenerateForegroundInstanceMaskRequest` / `VNInstanceMaskObservation.generateMaskedImage` 生成透明背景前景图，低版本或识别失败回退原图；新增测试覆盖配方、摘要、文件名和保存正文。本地验证通过：`git diff --check`、`plutil -lint`、`xmllint --noout`、CI YAML 解析、旧 Swift parser 覆盖 `Memo.swift` / `ImageEditRenderer.swift` / `ImageEditorView.swift` / `SomeTests.swift`。完整 Vision API typecheck 和 XCTest 以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-23：阶段 17 远端测试兼容性修复已整合：搜索内容筛选测试不再依赖集合顺序，音频/视频素材测试改用 UTType conforms 判断，视频缩略图缓存 key 测试通过取帧时间验证差异，`MemoBackupPackage` 改为只在导入/导出方法上使用 `@MainActor`，避开纯常量的 actor 隔离误报，衣橱天气洞察保留含标点的原始天气短语。本地验证通过：`git diff --check`、`plutil -lint`、旧 Swift parser 覆盖 `MemoSearchQuery.swift` / `VideoThumbnailGenerator.swift` / `WardrobeInsightEngine.swift` / `SomeTests.swift`；最新 GitHub Actions run `28009254154` 仍在 Run tests，需推送后继续复验。
- 2026-06-23：GitHub Actions run `28010154822` 的 XCTest 失败已定位为手帐导出附件素材断言没有考虑中文文件名 URI percent-encoding；测试已改为用 `attachment.referenceURI` 精确匹配素材 URI，并检查导出 PNG/PDF 类型。素材库视频缩略图缓存维护新增 `VideoThumbnailGenerator.sourceURLs(in:)` 和素材库出现/变化时的后台预热、孤儿缓存清理。本地验证通过：`git diff --check`、`plutil -lint`、旧 Swift parser 覆盖 `SharedAttachmentStore.swift` / `AttachmentReferenceResolver.swift` / `VideoThumbnailGenerator.swift` / `SomeTests.swift`；`ContentView.swift` 仍受本机 Swift 5.4 既有 `await` 语法限制，完整 UI 编译以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-23：GitHub Actions run `28011472819` 对应 `3951815` 已通过 Build and test。阶段 17 的远端 CI 阻塞已解除；后续只剩 GitHub Actions Node 20 deprecation 警告清理，本轮已将 `actions/checkout@v4` 升级为 `actions/checkout@v5`。
- 2026-06-23：阶段 48 已将 `.github/workflows/ios-testflight.yml` 的 checkout action 从 v4 对齐到 v5；本地已用 YAML 解析、checkout 版本 grep 和 `git diff --check` 验证。实际 Archive / Upload to TestFlight 仍需要 Apple 证书、provisioning profile 和 App Store Connect API secrets 后手动触发。
- 2026-06-23：GitHub Actions runs `28018991662`、`28019064703`、`28019499635` 的 Build for simulator 均通过，Run tests 失败 annotation 指向 `appintentsmetadataprocessor --module-name ZIPFoundation`。本轮将 Run tests 阶段改为临时移除 ZIPFoundation package 引用并启用 `CI_DISABLE_ZIP_BACKUP` stub，同时跳过 ZIP 包专属 XCTest；本地已验证 YAML、临时 pbxproj 改写、CI stub parse 和 `git diff --check`。正式 build / TestFlight 不启用该 flag，仍覆盖 ZIPFoundation 真实构建。
- 2026-06-23：阶段 51/52 已补完整备份摘要、附件 byteCount、手帐照片滤镜/相框和滤镜 helper 编译收口。本地验证通过：`xcrun swiftc -parse some/Models/Memo.swift some/Models/MemoBackupArchive.swift some/Utilities/ScrapbookRenderer.swift some/Views/ScrapbookEditorView.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse -D CI_DISABLE_ZIP_BACKUP some/Utilities/MemoBackupPackage.swift SomeTests/SomeTests.swift`、`git diff --check`、`plutil -lint some.xcodeproj/project.pbxproj some/Info.plist some/PrivacyInfo.xcprivacy SomeShareExtension/Info.plist`、`xmllint --noout some.xcodeproj/xcshareddata/xcschemes/some.xcscheme`、`.github/workflows/ios-ci.yml` / `.github/workflows/ios-testflight.yml` Ruby YAML 解析。单独解析 `SettingsView.swift` 仍会被本机 Swift 5.4 对既有 `if let` shorthand 和 `await` 语法限制阻止，完整 UI/XCTest 以 GitHub Actions Xcode 16.4 为准。
- 2026-06-23：GitHub Actions run `28022556308` 的 Build for simulator 已通过，Run tests 失败 annotation 只剩一个搜索日期解析相关 `XCTAssertTrue failed`。本轮将 `MemoSearchQueryParser.dateRange` 的 Gregorian calendar 显式固定到 `TimeZone.current`，并把测试改为比较完整起始日期，避免只看月/日或依赖默认 calendar 时区。本地验证通过：`xcrun swiftc -parse some/Utilities/MemoSearchQuery.swift SomeTests/SomeTests.swift`、`git diff --check`、`plutil -lint some.xcodeproj/project.pbxproj some/Info.plist some/PrivacyInfo.xcprivacy SomeShareExtension/Info.plist`、workflow YAML 解析。
- 2026-06-23：阶段 53 新增 `.somebackup` 文件清单摘要读取。TDD 红灯验证受本机旧工具链限制：`xcrun swiftc -typecheck some/Models/Memo.swift some/Models/MemoBackupArchive.swift some/Utilities/MemoBackupPackage.swift SomeTests/SomeTests.swift` 先失败于 `no such module 'ZIPFoundation'`，无法在本地跑完整 ZIP XCTest；测试仍已先于实现写入。当前已通过可执行验证：`xcrun swiftc -parse some/Utilities/MemoBackupPackage.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse -D CI_DISABLE_ZIP_BACKUP some/Utilities/MemoBackupPackage.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse some/Utilities/MemoSearchQuery.swift SomeTests/SomeTests.swift`、`git diff --check`、`plutil -lint some.xcodeproj/project.pbxproj some/Info.plist some/PrivacyInfo.xcprivacy SomeShareExtension/Info.plist`、`xmllint --noout some.xcodeproj/xcshareddata/xcschemes/some.xcscheme`、workflow YAML 解析。完整 ZIPFoundation typecheck、UI 编译和 XCTest 仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-23：GitHub Actions run `28023921262` 仍失败在测试阶段，公开页面只显示 `XCTAssertTrue failed`，logs API 返回 403。已把日期 parser 测试拆成可定位的 `XCTEqual` 断言，并扩大 CI 失败摘要 grep 范围；本轮需重新跑 `swiftc -parse`、`git diff --check`、plist/scheme/workflow 校验并推送后继续观察新 run。
- 2026-06-23：阶段 54 禅定模式快速记录已完成本地收束验证：`xcrun swiftc -parse some/Utilities/ZenDraftStats.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse some/Utilities/MemoSearchQuery.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse -D CI_DISABLE_ZIP_BACKUP some/Utilities/MemoBackupPackage.swift some/Utilities/ZenDraftStats.swift SomeTests/SomeTests.swift`、`git diff --check`、`plutil -lint some.xcodeproj/project.pbxproj some/Info.plist some/PrivacyInfo.xcprivacy SomeShareExtension/Info.plist`、`xmllint --noout some.xcodeproj/xcshareddata/xcschemes/some.xcscheme`、workflow YAML 解析。`ContentView.swift` 单文件 parse 仍被本机旧 Swift 5.4 对既有 `await` 语法限制阻止，完整 UI 编译以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-23：CI 裸断言诊断增强已通过本地验证：`xcrun swiftc -parse some/Utilities/MemoSearchQuery.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse -D CI_DISABLE_ZIP_BACKUP some/Utilities/MemoBackupPackage.swift some/Utilities/ZenDraftStats.swift SomeTests/SomeTests.swift`、`git diff --check`、workflow YAML 解析。完整 XCTest 仍等待新 GitHub Actions run。
- 2026-06-23：GitHub Actions run `28029991853` 通过增强摘要确认业务测试已继续推进，最终失败线仍为 `appintentsmetadataprocessor`。本轮已验证 CI 临时 pbxproj 改写后 `plutil -lint` 通过，且 AppIntents build files、ZIPFoundation、扩展嵌入和分享/小组件 target dependency 均不再出现在测试用 pbxproj；小组件本地验证通过：`xcrun swiftc -parse some/Utilities/WidgetSnapshotStore.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse -D SOME_WIDGET_EXTENSION some/Utilities/SharedMemoStorage.swift some/Utilities/WidgetSnapshotStore.swift SomeWidget/SomeWidget.swift`、`plutil -lint SomeWidget/Info.plist SomeWidget/SomeWidget.entitlements some.xcodeproj/project.pbxproj`、`git diff --check`。
- 2026-06-23：工作汇报稿顺序修复已通过本地验证：`xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse -D CI_DISABLE_ZIP_BACKUP some/Utilities/MemoBackupPackage.swift some/Utilities/ZenDraftStats.swift some/Utilities/WidgetSnapshotStore.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/workflow 校验。完整 XCTest 等待新 GitHub Actions run。
- 2026-06-23：阶段 56 Widget TestFlight 签名链路本地验证通过：`.github/workflows/ios-ci.yml` / `.github/workflows/ios-testflight.yml` Ruby YAML 解析、`plutil -lint some.xcodeproj/project.pbxproj some/Info.plist some/PrivacyInfo.xcprivacy SomeShareExtension/Info.plist SomeShareExtension/SomeShareExtension.entitlements SomeWidget/Info.plist SomeWidget/SomeWidget.entitlements`、`xmllint --noout some.xcodeproj/xcshareddata/xcschemes/some.xcscheme`、`git diff --check`。另用模拟环境生成 TestFlight `ExportOptions.plist` 并通过 `plutil -lint` / `plutil -p` 确认主 App、`.share` 和 `.widget` 三个 bundle ID 都写入 provisioningProfiles。
- 2026-06-23：阶段 58 引用批注正文与筛选本地验证通过：`xcrun swiftc -parse some/Utilities/MemoReferenceParser.swift some/Models/Memo.swift some/Utilities/SharedAttachmentStore.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse some/Utilities/MemoSearchQuery.swift some/Utilities/MemoReferenceParser.swift some/Utilities/WorkLogSourceFilterEngine.swift some/Models/Memo.swift some/Utilities/SharedAttachmentStore.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-23：阶段 59 重复引用批注解析与 CI 预防修复本地验证通过：先用 Swift 探针复现阶段 58 HEAD 只返回第一条重复批注的失败，再验证修复后返回 `["第一条依据", "第二条依据"]`；随后通过 `xcrun swiftc -parse some/Utilities/MemoReferenceParser.swift some/Utilities/MemoSearchQuery.swift some/Stores/MemoStore.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift some/Utilities/MemoReferenceParser.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。GitHub 公共 API 当前返回 core rate limit，最新 run `28033567402` 待窗口恢复后继续复验。
- 2026-06-23：阶段 60 工作日志团队汇报模板导出本地验证通过：新增测试覆盖 `WorkLogExporter.reportDraft` 的 `.standup` 与 `.projectBrief` 输出；本地通过 `xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse some/Utilities/MemoSearchQuery.swift some/Utilities/MemoReferenceParser.swift some/Utilities/WorkLogSourceFilterEngine.swift some/Models/Memo.swift some/Utilities/SharedAttachmentStore.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-23：阶段 61 本地 AI 记忆档案与禅定偏好本地验证通过：`xcrun swiftc -parse some/AI/AIMemoryProfile.swift some/AI/AIInsightComposer.swift some/Utilities/DateFormatters.swift some/Utilities/TagParser.swift some/Utilities/MemoTaskParser.swift some/Utilities/MemoReferenceParser.swift some/Utilities/SharedAttachmentStore.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`git diff --check`、`plutil -lint some.xcodeproj/project.pbxproj some/Info.plist some/PrivacyInfo.xcprivacy SomeShareExtension/Info.plist SomeWidget/Info.plist`。`AIWorkspaceView.swift` 单文件 parse 被本机 Swift 5.4 对既有 async/await 和新式 `if let` 语法限制阻止，完整 UI 编译和 XCTest 仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-23：阶段 62 AI 工作台本地相关搜索本地验证通过：临时 Swift 探针执行 `SemanticSearchEngine.localSearch` 的同等逻辑，确认归档记录排除、分数非零、按分数/时间排序；`xcrun swiftc -parse some/AI/SemanticSearchEngine.swift some/Models/Memo.swift SomeTests/SomeTests.swift` 在本机旧 Swift 5.4 下被既有 `async` / `try await` 语法阻止，无法作为完整绿灯。本轮另通过 `xcrun swiftc -parse some/AI/AIMemoryProfile.swift some/AI/AIInsightComposer.swift some/Utilities/ZenDraftStats.swift some/Utilities/DateFormatters.swift some/Utilities/TagParser.swift some/Utilities/MemoTaskParser.swift some/Utilities/MemoReferenceParser.swift some/Utilities/SharedAttachmentStore.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-23：阶段 63 语音转写语言选择本地验证通过：临时 Swift 探针执行 `AudioTranscriptionLanguage` 与 `AudioTranscriber.memoText` 同等逻辑，确认未知值回退自动、普通话/英语 locale 和英语标题标注；`xcrun swiftc -parse some/Utilities/AudioTranscriber.swift some/Utilities/SharedAttachmentStore.swift SomeTests/SomeTests.swift` 在本机旧 Swift 5.4 下被既有 `async` / `try await` 语法阻止，无法作为完整绿灯。本轮另通过 `git diff --check`、plist/scheme/workflow 校验。完整语音转写编译、授权和识别效果仍需 GitHub Actions/Xcode 16.4 与真机/模拟器验证。
- 2026-06-23：阶段 64/65 本地 AI 搜索命中解释与连续扫描 OCR 本地验证通过：`xcrun swiftc -parse some/AI/AIMemoryProfile.swift some/AI/AIInsightComposer.swift some/Utilities/ZenDraftStats.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`rg -n "matchedTerms|containsCompactScript|扫描文字|pageNumber|VNDocumentCameraViewController|DocumentScannerView" some/AI/SemanticSearchEngine.swift some/Views/AIWorkspaceView.swift some/Views/QuickCaptureView.swift some/Utilities/ImageTextRecognizer.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。完整 SwiftUI/UIKit/VisionKit 编译、连续扫描权限和 XCTest 仍以 GitHub Actions/Xcode 16.4 或真机为准。
- 2026-06-23：阶段 66 AI embedding 缓存与行动复盘导出本地验证通过：临时 Swift 5.4 兼容探针确认 embedding cache 对重复输入只产生去重 missing requests，二次 lookup 命中缓存并按原输入顺序回填；另用最小桩类型编译生产 `WorkLogSourceFilterEngine.swift`，确认 `.actionReview` 输出“行动复盘/已完成/待跟进/风险/记录要点”格式。本轮还通过 `xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。`xcodebuild -version` 在当前机器被 Command Line Tools 环境阻止，提示需要完整 Xcode；`SemanticSearchEngine.swift` 含 actor/async，旧 Swift 5.4 不能完整 parse/typecheck，完整 XCTest 与 SwiftUI 编译仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-24：阶段 67 AI embedding 跨启动本地缓存本地验证通过：临时 Swift 5.4 兼容探针确认快照 JSON 不含“产品路线”原文、恢复后同模型命中、不同模型 miss、磁盘保存/加载不含原文；生产代码已把磁盘写入降级为 best-effort，避免缓存文件失败影响语义搜索主流程。本轮通过 `/tmp/semantic-cache-stage67-green`、`xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、`xcrun swiftc -parse some/AI/AIMemoryProfile.swift some/AI/AIInsightComposer.swift some/Utilities/ZenDraftStats.swift some/Utilities/DateFormatters.swift some/Utilities/TagParser.swift some/Utilities/MemoTaskParser.swift some/Utilities/MemoReferenceParser.swift some/Utilities/SharedAttachmentStore.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。`SemanticSearchEngine.swift` 仍含 `CryptoKit`、actor/async，本机旧 Swift 5.4 不能完整 parse/typecheck；完整 XCTest 与 SwiftUI 编译仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-24：阶段 68 AI embedding 缓存容量裁剪本地验证通过：先用红灯探针确认 `SemanticEmbeddingCache(maxEntryCount:)` 缺失，再用 `/tmp/semantic-cache-stage68-green` 验证 2 条上限下保留最近命中的第一/第三条、淘汰第二条，并用 `/tmp/semantic-cache-stage67-regression` 验证阶段 67 旧 `[key: vector]` JSON 快照仍可解码。本轮还通过 `xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、AI/记忆相关可解析文件 parse、`git diff --check`、plist/scheme/workflow 校验。`SemanticSearchEngine.swift` 仍含 `CryptoKit`、actor/async，本机旧 Swift 5.4 不能完整 parse/typecheck；完整 XCTest 与 SwiftUI 编译仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-24：阶段 69 设置页 AI 语义缓存清理入口本地验证通过：`rg -n "清除 AI 语义缓存|isShowingAICacheClearConfirmation|clearEmbeddingCache|AICache|AI 语义搜索会在本机保存" some/Views/SettingsView.swift README.md findings.md progress.md task_plan.md verification.md` 确认入口、确认弹窗、清理调用和隐私说明；另通过 `xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、AI/记忆相关可解析文件 parse、`git diff --check`、plist/scheme/workflow 校验。`SettingsView.swift` 为 SwiftUI/UIKit 文件，完整编译仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-24：阶段 70 设置页 AI 语义缓存摘要本地验证通过：先用红灯探针确认 `SemanticEmbeddingDiskCache.summary` 缺失，再用 `/tmp/semantic-cache-stage70-green` 验证空缓存为 0、保存快照后条目数为 1 且文件大小非零、删除后恢复 0；`rg -n "summary|aiCacheSummary|aiCacheSummaryText|ByteCountFormatter|AI 语义缓存：" some/AI/SemanticSearchEngine.swift some/Views/SettingsView.swift SomeTests/SomeTests.swift` 确认设置页摘要接线。本轮还通过 `xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、AI/记忆相关可解析文件 parse、`git diff --check`、plist/scheme/workflow 校验。`SettingsView.swift` 为 SwiftUI/UIKit 文件，完整编译仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-24：阶段 71 App Store 隐私文案同步本地验证通过：`rg -n "AICache|SHA-256|1000|OpenAI API|App Group|清除|embedding|AI 语义缓存" AppStore/privacy-policy.md AppStore/app-store-metadata.md AppStore/submission-checklist.md` 确认隐私政策、元数据和上架清单覆盖本机缓存、指纹 key、上限、OpenAI 发送边界和设置页清除；另通过 Python 关键字检查、`git diff --check`、plist/scheme/workflow 校验。
- 2026-06-24：阶段 72 首次记录模板引导本地验证：先写 `testQuickCaptureStarterSuggestionsSeedFirstMemo`；本机 `xcrun swiftc -parse some/Views/QuickCaptureView.swift SomeTests/SomeTests.swift` 被既有 async/await 和新式 `if let` 语法限制拦截，不能作为红/绿完整证据。本轮改用 `rg -n "QuickCaptureStarterSuggestion|shouldShowStarterSuggestions|applyStarterSuggestion|记录想法|保存链接|写工作日志" some/Views/QuickCaptureView.swift SomeTests/SomeTests.swift` 确认模型、UI 接线和测试存在；另通过 `git diff --check`、plist/scheme/workflow 校验。完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-24：阶段 73 工作日志 AI 润色导出本地验证：`testWorkLogPolishPromptPreservesFactsAndStructure` 覆盖目标读者、不要编造、保留项目名/日期/数字和原始草稿；`rg -n "AI 润色汇报|exportAIPolishedWorkLog|WorkLogPolishComposer|isPolishingWorkLog|makeClient\\(\\)\\.generateText" some/Views/ContentView.swift some/AI/AIInsightComposer.swift SomeTests/SomeTests.swift` 确认 prompt、菜单、状态、OpenAI 调用和测试接线；另通过 `xcrun swiftc -parse some/AI/AIInsightComposer.swift some/AI/AIMemoryProfile.swift some/Utilities/DateFormatters.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。`ContentView.swift` 含大量 SwiftUI/concurrency，完整 UI 编译仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-24：阶段 74 工作日志团队周报模板本地验证：`testWorkLogExporterBuildsTeamWeeklyReportDraft` 覆盖团队周报输出项目/日期、本周成果、关键影响、风险/需要协作和下周重点；`rg -n "teamWeekly|团队周报|work log draft|工作汇报草稿|AI 润色" some/Utilities/WorkLogSourceFilterEngine.swift some/Views/ContentView.swift SomeTests/SomeTests.swift README.md AppStore/privacy-policy.md AppStore/app-store-metadata.md AppStore/submission-checklist.md` 确认导出器、菜单、测试和隐私文案接线；另通过 `xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。`ContentView.swift` 的完整 SwiftUI 编译仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-24：阶段 75 本地 AI 搜索 NaturalLanguage 分词增强本地验证：`testLocalSemanticSearchTokenizesCompactChinesePhrases` 覆盖连续中文短语分词匹配；临时 `/tmp/local_search_nl_probe.swift` 验证 `NLTokenizer` 会把“团队周报模板”切为“团队/周报/模板”，并且命中词展示过滤低权重“队周”碎片；另通过 `rg -n "NaturalLanguage|naturalLanguageTokens|testLocalSemanticSearchTokenizesCompactChinesePhrases|hasStrongTerms" some/AI/SemanticSearchEngine.swift SomeTests/SomeTests.swift`、`git diff --check` 校验。`SemanticSearchEngine.swift` 含 actor/现代 `try await`，本机旧 Swift 5.4 parser 不能完整证明，完整 XCTest 仍以 GitHub Actions/Xcode 16.4 为准。
- 2026-06-24：阶段 76 App Group 旧存储迁移测试本地验证通过：`testSharedStorageCopiesLegacyMemoFilesIntoSharedDirectoryWithoutOverwriting` 覆盖旧 JSON 备份、SQLite、WAL、SHM 迁入共享目录且不覆盖已有共享主 JSON；临时 Swift 探针输出 `shared migration probe: OK`，并通过 `xcrun swiftc -parse some/Utilities/SharedMemoStorage.swift SomeTests/SomeTests.swift`、`rg -n "testSharedStorageCopiesLegacyMemoFilesIntoSharedDirectoryWithoutOverwriting|migrateFromStandardDocumentsIfNeeded|copyIfNeeded|legacy wal|legacy shm" some/Utilities/SharedMemoStorage.swift SomeTests/SomeTests.swift findings.md progress.md verification.md`、`git diff --check`、plist/scheme/workflow 校验。完整 Share Extension 真机保存仍以 Xcode/Apple Team 验证为准。
- 2026-06-24：阶段 77 App Shortcuts 打开常用工作区本地接线验证：`testAppShortcutRouteStoreOpensAndConsumesDestination` 覆盖 App Group UserDefaults 路由请求、切换到工作日志、清空旧搜索和只消费一次；`rg -n "OpenZenIntent|OpenWorkLogIntent|OpenWardrobeIntent|OpenAIWorkspaceIntent|AppShortcutRouteStore|openPendingShortcutDestinationIfNeeded|打开专注|打开工作日志|打开衣橱|打开 AI" some/AppIntents some/Stores/MemoStore.swift some/SomeApp.swift SomeTests/SomeTests.swift README.md` 确认 App Shortcuts、启动/激活消费和文档接线；`README.md`、`AppStore/submission-checklist.md`、`docs/online-build-and-release.md` 已同步 TestFlight 自测项。另通过 `xcrun swiftc -parse some/Stores/MemoStore.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。本机只有 Command Line Tools，`xcodebuild -version` 报 active developer directory 不是完整 Xcode，`gh` 不存在；App Intents 源在旧 Swift parser 下会被 async/AppIntents 语法拦截，完整编译和远端 CI 状态以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 81 每日回顾摘要本地验证：`testReviewSummaryCountsTodayStreakAndReviewBacklog` 覆盖今日记录数、连续记录天数、7 天前可回顾旧记录数量和提示文案；回顾页顶部新增“今日/连续/可回顾”摘要面板。阶段 80 远端 run `28046929987` 的公开 annotation 显示 `Run tests` 在 12 分钟超时，未返回业务断言失败；本轮将 `.github/workflows/ios-ci.yml` 测试 timeout 调整到 20 分钟。完整 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 78/79 衣橱 Open-Meteo 天气查询与图片裁剪方向本地验证：`testOpenMeteoWeatherServiceBuildsRequestURLsAndSummary` 覆盖 geocoding/forecast URL、位置 display name、天气码、温度范围、降雨概率和备注文案，临时 Swift 探针输出 `open meteo weather probe ok`；`testImageEditRendererAppliesCropTransformBeforeCropping`、`testImageEditRendererBuildsTransformedCropPreviewImage`、`testImageEditRecipeRoundTripsFullRecipe` 和 image edit store 测试覆盖裁剪前旋转/翻转、预览尺寸、文件名 token 和保存正文“方向”。本地另通过 `xcrun swiftc -parse some/Utilities/OpenMeteoWeatherService.swift some/Models/Memo.swift some/Utilities/ImageEditRenderer.swift some/Views/ImageEditorView.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 和文档关键词校验。`ContentView.swift` 单文件 parse 被本机旧 Swift 5.4 parser 的既有 `await`/`async` 语法限制拦截，UIKit 图像可执行探针被 Command Line Tools 缺 iOS SDK 拦截，完整 UI/图像编译以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 79 图片编辑裁剪方向与翻转本地验证：`testImageEditRendererAppliesCropTransformBeforeCropping` 覆盖裁剪前右旋和水平翻转，`testImageEditRendererBuildsTransformedCropPreviewImage` 覆盖裁剪预览复用同一变换；通过 `xcrun swiftc -parse some/Models/Memo.swift some/Stores/MemoStore.swift some/Utilities/ImageEditRenderer.swift some/Views/ImageEditorView.swift SomeTests/SomeTests.swift`、`rg -n "CropTransform|cropTransform|右旋|水平翻转|方向|flipH|ImageEditRendererAppliesCropTransform" some SomeTests/SomeTests.swift`、`git diff --check`、plist/scheme/workflow 校验。完整 UIImage renderer 像素测试仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 80 衣橱胶囊打包建议本地验证：先用临时 Swift 探针确认旧引擎缺少 `packing-capsule` 会触发 `missing capsule suggestion` 红灯，再实现后用同一场景输出 `capsule packing probe: OK`；新增 `testWardrobePackingSuggestionsIncludeCapsuleWardrobeSet` 覆盖胶囊打包标题、目的地、天气、基础色/跨场景单品、低重复穿着和备注文案。本地另通过 `xcrun swiftc -parse some/Utilities/WardrobeInsightEngine.swift some/Models/Memo.swift some/Stores/MemoStore.swift SomeTests/SomeTests.swift` 与 `rg -n "packing-capsule|胶囊打包|capsule|testWardrobePackingSuggestionsIncludeCapsuleWardrobeSet|基础色|可互相搭配" some/Utilities/WardrobeInsightEngine.swift SomeTests/SomeTests.swift` 校验；完整 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 82 旧记录回看入口本地验证：新增 `testReviewBacklogMemosReturnsOldestActiveCandidates` 覆盖 7 天前旧记录候选、归档排除和从旧到新排序；`ReviewView` 新增“旧记录回看”区块，复用 `NavigationLink` 打开详情。本地通过 `xcrun swiftc -parse some/Models/Memo.swift some/Stores/MemoStore.swift some/Views/ReviewView.swift SomeTests/SomeTests.swift`、`rg -n "reviewBacklogMemos|旧记录回看|testReviewBacklogMemosReturnsOldestActiveCandidates|阶段 82|可点击回看入口|LRStreakKit|Spaced-Repetition" some/Stores/MemoStore.swift some/Views/ReviewView.swift SomeTests/SomeTests.swift README.md findings.md task_plan.md progress.md verification.md`、`git diff --check`、plist/workflow YAML 校验。`xcrun swiftc -typecheck some/Models/Memo.swift some/Stores/MemoStore.swift SomeTests/SomeTests.swift` 在当前 Command Line Tools 环境先失败于 `no such module 'UIKit'`，完整 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 83 链接导入跟踪参数去重本地验证：新增 `testLinkExtractorDeduplicatesTrackingParameterVariants`、`testSharedMemoComposerDeduplicatesTrackingParameterVariants` 与 `testSharedMemoComposerDoesNotAppendEquivalentTrackingURLAlreadyInText`，覆盖输入卡片 URL 检测、系统分享文本合成和正文已有等价链接路径；`LinkExtractor.deduplicationKey` 只用于去重，保存内容保留第一条原始 URL。本地通过 `xcrun swiftc -parse some/Utilities/LinkExtractor.swift some/Utilities/SharedMemoTextComposer.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/MemoReferenceParser.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`rg -n "testLinkExtractorDeduplicatesTrackingParameterVariants|testSharedMemoComposerDeduplicatesTrackingParameterVariants|testSharedMemoComposerDoesNotAppendEquivalentTrackingURLAlreadyInText|deduplicationKey|utm_|fbclid|gclid|mc_cid" some/Utilities/LinkExtractor.swift some/Utilities/SharedMemoTextComposer.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/workflow YAML 校验。完整 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 84 网页摘录卡跟踪参数去重本地验证：先用临时 Swift 探针确认 `647f725` 基线的 `LinkExtractor.webClips(in:)` 对同一网页的 tracking URL 变体返回 2 个 clip，再修复为复用 `deduplicationKey` 后输出 `webclip red-green probe ok`；新增 `testLinkExtractorDeduplicatesWebClipTrackingParameterVariants` 覆盖网页摘录卡解析保留第一条原始 URL 和摘要。本地通过 `xcrun swiftc -parse some/Utilities/SharedAttachmentStore.swift some/Utilities/MemoReferenceParser.swift some/Utilities/LinkExtractor.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/workflow YAML 校验。完整 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 85 工作日志会议纪要导出本地验证：新增 `testWorkLogExporterBuildsMeetingMinutesDraft` 覆盖会议纪要导出项目/日期、议题/结论、待确认、行动项和备注；`WorkLogExporter.ReportDraftStyle.meetingMinutes` 和 `WorkLogExportFormat.meetingMinutesDraft` 已接到工作日志导出菜单。本地通过 `xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、`rg -n "meetingMinutes|meetingMinutesDraft|会议纪要|testWorkLogExporterBuildsMeetingMinutesDraft" some/Utilities/WorkLogSourceFilterEngine.swift some/Views/ContentView.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md`、`git diff --check`、plist/workflow YAML 校验。`xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift some/Views/ContentView.swift SomeTests/SomeTests.swift` 在当前旧 Swift 5.4 环境被既有 `await`/`async` 语法限制拦截，完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 86 OCR 置信度保存本地验证：先用临时 Swift 探针确认旧接口缺少 `ImageTextRecognizer.RecognizedLine` 会编译失败；新增 `testImageTextRecognizerBuildsMemoTextWithConfidenceSummary` 覆盖重复 OCR 行去重后平均/最低置信度。实现后通过 `xcrun swiftc -parse -Xfrontend -enable-experimental-concurrency some/Utilities/SharedAttachmentStore.swift some/Utilities/MemoReferenceParser.swift some/Utilities/ImageTextRecognizer.swift SomeTests/SomeTests.swift`、`rg -n "RecognizedLine|recognizeTextLines|置信度|testImageTextRecognizerBuildsMemoTextWithConfidenceSummary" some SomeTests/SomeTests.swift`、`git diff --check`、plist/workflow YAML 校验。`QuickCaptureView.swift` / `MemoDetailView.swift` 单文件 parse 被本机旧 Swift 5.4 的既有现代 Swift 语法限制拦截，完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 87 衣橱长途天气打包增强本地验证：新增 `testWardrobePackingSuggestionsAddWeatherEssentialsForLongTrips` 覆盖 4 天晴热阵雨旅行的可替换上装、防水风衣、防水凉鞋、折叠伞、太阳帽和提示文案；`WardrobeInsightEngine` 新增 `PackingWeatherNeeds` 识别高温、阵雨、低温并扩展打包建议。本地通过 `xcrun swiftc -parse some/Utilities/WardrobeInsightEngine.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`rg -n "PackingWeatherNeeds|testWardrobePackingSuggestionsAddWeatherEssentialsForLongTrips|高温多带|阵雨补|防水风衣|折叠伞" some/Utilities/WardrobeInsightEngine.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md`、`git diff --check`、plist/workflow YAML 校验。阶段 85 远端 CI run `28075916766` 对 `9efa778` 已 completed/success；当前阶段完整 XCTest 仍待推送后由 GitHub Actions/Xcode 16 验证。
- 2026-06-24：阶段 88 OCR 批量校对清单本地验证：先用临时 Swift 探针确认旧 `ClipFragmentExtractor` 缺少 `ocrProofreadingChecklist` 会编译失败；新增 `testClipFragmentExtractorBuildsOCRProofreadingChecklist` 覆盖多段 OCR 去重、跳过置信度/扫描页/附件行和来源聚合；详情页图片文字区域新增“生成 OCR 校对清单”按钮。本地通过 `xcrun swiftc -parse some/Utilities/SharedAttachmentStore.swift some/Utilities/MemoReferenceParser.swift some/Utilities/LinkExtractor.swift some/Utilities/ClipFragmentExtractor.swift SomeTests/SomeTests.swift`、`rg -n "ocrProofreadingChecklist|OCR校对|生成 OCR 校对清单|testClipFragmentExtractorBuildsOCRProofreadingChecklist|阶段 88" some SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md`、`git diff --check`、plist/workflow YAML 校验。完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 89 低置信度 OCR 待校对筛选本地验证：先用临时 Swift 探针确认 HEAD 基线 `MemoContentFilter` 缺少 `.ocrReview` 会编译失败；新增/复用 `testSearchQueryParserExtractsOCRReviewAliases`、`testSearchCanFilterLowConfidenceOCRForReview` 和 `testWorkLogSourceFilterEngineCanFilterOCRReviewCandidates` 覆盖别名解析、全局搜索和工作日志来源筛选。本轮实现后通过 `xcrun swiftc -parse -Xfrontend -enable-experimental-concurrency some/Utilities/DateFormatters.swift some/Utilities/SharedMemoStorage.swift some/Utilities/MemoSearchQuery.swift some/Utilities/LinkExtractor.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/MemoReferenceParser.swift some/Utilities/MemoTaskParser.swift some/Utilities/ClipFragmentExtractor.swift some/Utilities/WorkLogSourceFilterEngine.swift some/Models/Memo.swift some/Stores/MemoStore.swift SomeTests/SomeTests.swift`、`rg -n "ocrReview|ocr-review|待校对|needsOCRReview|testWorkLogSourceFilterEngineCanFilterOCRReviewCandidates" some SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md`、`git diff --check`、plist/workflow YAML 校验。`swiftc -typecheck` 在当前 Command Line Tools 环境会先失败于 UIKit/CoreGraphics 桥接和跨文件扩展依赖，完整 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 90 Markdown 代码块阅读渲染本地验证：先用临时 Swift 探针确认基线缺少 `MarkdownMemoBlockParser`；新增 `testMarkdownMemoBlockParserGroupsFencedCodeBlocks`、`testMarkdownMemoBlockParserKeepsUnclosedFenceAsCodeToEnd` 和 `testMarkdownMemoBlockParserAcceptsFenceWithoutLanguage` 覆盖代码块聚合、未闭合 fence、无语言 fence 和代码内任务不误识别。本轮实现后通过 `xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Views/MarkdownMemoTextView.swift SomeTests/SomeTests.swift`、`rg -n "MarkdownMemoBlockParser|MarkdownMemoCodeBlock|testMarkdownMemoBlockParser|Markdown 阅读渲染 v2|阶段 90" some SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md`、`git diff --check`、plist/workflow YAML 校验。当前 Command Line Tools 的旧 SwiftUI SDK 不支持本项目使用的部分现代 `foregroundStyle` / `textSelection` / `AttributedString(markdown:)` typecheck，完整 SwiftUI 编译仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 91 Markdown 标题与引用块阅读渲染本地验证：新增 `testMarkdownMemoBlockParserExtractsHeadingsAndKeepsTagsAsLines` 和 `testMarkdownMemoBlockParserGroupsBlockquotes`，覆盖标题等级、`#标签/子类` 不误判标题、连续引用合并和后续正文行号。本轮实现后通过 `xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Views/MarkdownMemoTextView.swift SomeTests/SomeTests.swift`、`rg -n "MarkdownMemoHeadingBlock|MarkdownMemoQuoteBlock|testMarkdownMemoBlockParserExtractsHeadings|testMarkdownMemoBlockParserGroupsBlockquotes|Markdown 阅读渲染 v3|阶段 91" some SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md`、`git diff --check`、plist/workflow YAML 校验。完整 SwiftUI 编译仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 92 OCR 版面分区摘要本地验证：先用红灯探针确认旧 `RecognizedLine` 缺少 `region` 参数会在 typecheck 阶段报 `extra argument 'region' in call`；新增 `testImageTextRecognizerBuildsMemoTextWithLayoutSections` 和 `testImageTextRecognizerKeepsOldMemoFormatWithoutLayoutRegions`，覆盖有 Vision 行框位置时输出左/右栏和顶部/中部/底部分区摘要、无位置数据时不改变旧 OCR memo 格式。本轮实现后通过 `xcrun swiftc -parse -Xfrontend -enable-experimental-concurrency some/Utilities/SharedAttachmentStore.swift some/Utilities/MemoReferenceParser.swift some/Utilities/ImageTextRecognizer.swift SomeTests/SomeTests.swift`、`rg -n "ImageTextLayoutSection|版面分区|testImageTextRecognizerBuildsMemoTextWithLayoutSections|region: ImageTextRegion\\?|imageRegion\\(fromVisionBoundingBox" some/Utilities/ImageTextRecognizer.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md`、`git diff --check`、plist/workflow YAML 校验。完整 XCTest 与 Vision OCR 真机/模拟器行为仍以 GitHub Actions/Xcode 16 和设备验证为准。
- 2026-06-24：阶段 95 OCR 行级阅读顺序整理本地验证：新增 `testImageTextRecognizerOrdersRecognizedLinesByLayoutPosition`，覆盖 Vision 返回顺序打乱时，带 `ImageTextRegion` 的 OCR 行会按中心点从上到下、同一行从左到右保存。本轮实现后通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse -Xfrontend -enable-experimental-concurrency some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/MemoReferenceParser.swift some/Utilities/ImageTextRecognizer.swift SomeTests/SomeTests.swift`、`rg -n "readingOrderedLines|testImageTextRecognizerOrdersRecognizedLinesByLayoutPosition|OCR 行级阅读顺序|图片/截图/扫描 OCR v8|阶段 95" some/Utilities/ImageTextRecognizer.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md docs/open-source-reuse-audit.md`、`git diff --check`、plist/workflow YAML 校验。完整 Vision OCR 行框顺序仍以 GitHub Actions/Xcode 16 和真机/模拟器验证为准。
- 2026-06-24：阶段 93 Markdown 表格与分隔线阅读渲染本地验证：新增 `testMarkdownMemoBlockParserGroupsTables`、`testMarkdownMemoBlockParserKeepsPipeTextWithoutTableSeparatorAsLines` 和 `testMarkdownMemoBlockParserExtractsDividers`，覆盖 GFM 风格表格聚合、普通管道文本不误判、分隔线 block 和后续正文行号。本轮实现后通过 `xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Views/MarkdownMemoTextView.swift SomeTests/SomeTests.swift`、`rg -n "MarkdownMemoTableBlock|MarkdownMemoDividerBlock|testMarkdownMemoBlockParserGroupsTables|testMarkdownMemoBlockParserExtractsDividers|Markdown 阅读渲染 v4|阶段 93" some/Views/MarkdownMemoTextView.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md docs/open-source-reuse-audit.md`、`git diff --check`、plist/workflow YAML 校验。完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 94 Markdown 附件卡片预览本地验证：新增 `testMarkdownMemoBlockParserExtractsAttachmentCards`、`testMarkdownMemoBlockParserKeepsAttachmentReferencesInsideCodeBlocks`、`testMemoReferenceParserKeepsAttachmentReferencesVisibleInReadableText` 和 `testMemoReferenceParserMapsVisibleLineToOriginalLineAfterHiddenReference`，覆盖独立附件引用行转为 attachment block、后续任务行号不变、代码块内附件引用不被卡片化、隐藏 memo 引用后附件仍在可读正文中且任务勾选能映射回原始行。本轮实现后通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/MemoReferenceParser.swift some/Views/AttachmentPreviewList.swift some/Views/MarkdownMemoTextView.swift some/Views/MemoDetailView.swift SomeTests/SomeTests.swift`、`rg -n "MarkdownMemoAttachmentBlock|attachmentBlock|testMarkdownMemoBlockParserExtractsAttachmentCards|testMemoReferenceParserMapsVisibleLine|compactAttachmentPreviews|allowsAttachmentSharing|Markdown 阅读渲染 v5|阶段 94" some/Views/MarkdownMemoTextView.swift some/Views/MemoRowView.swift some/Views/MemoDetailView.swift some/Utilities/MemoReferenceParser.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md docs/open-source-reuse-audit.md`、`git diff --check`、plist/workflow YAML 校验。当前本机 Xcode 13.2.1 低于项目 iOS 16/Xcode 16 目标，`xcodebuild test` 找不到可用 iOS 16 目的地，`build-for-testing` 还会因 test host 和 ZIPFoundation/visionOS 条件编译失败；完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 96 Markdown 脚注定义阅读渲染本地验证：新增 `testMarkdownMemoBlockParserGroupsFootnotes` 和 `testMarkdownMemoBlockParserKeepsFootnotesInsideCodeBlocks`，覆盖连续 `[^id]: text` 脚注定义聚合、后续任务行号不变和代码块内脚注样式不误解析。本轮实现后通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/MemoReferenceParser.swift some/Views/AttachmentPreviewList.swift some/Views/MarkdownMemoTextView.swift SomeTests/SomeTests.swift`、`rg -n "MarkdownMemoFootnotesBlock|MarkdownMemoFootnoteItem|footnotesBlock|testMarkdownMemoBlockParserGroupsFootnotes|Markdown 阅读渲染 v6|阶段 96" some/Views/MarkdownMemoTextView.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md docs/open-source-reuse-audit.md`、`git diff --check`、plist/workflow YAML 校验。完整 SwiftUI 编译和 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 97 OCR 表单字段候选摘要本地验证：新增 `testImageTextRecognizerBuildsFieldCandidatesForFormLikeLines`、`testImageTextRecognizerSkipsFieldCandidatesForSingleLabelLine` 和 `testImageTextRecognizerSkipsURLSchemeFieldCandidates`，覆盖两行以上短字段生成“字段候选”、单个提示行不误判、URL scheme 不误判且原始识别文字保留。本轮实现后通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/ImageTextRecognizer.swift SomeTests/SomeTests.swift`、`rg -n "fieldCandidatesSummary|字段候选|testImageTextRecognizerBuildsFieldCandidatesForFormLikeLines|testImageTextRecognizerSkipsFieldCandidatesForSingleLabelLine|testImageTextRecognizerSkipsURLSchemeFieldCandidates|OCR v9|阶段 97" some/Utilities/ImageTextRecognizer.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md docs/open-source-reuse-audit.md`、`git diff --check`、plist/workflow YAML 校验。尝试执行 `xcodebuild test -project some.xcodeproj -scheme some -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:SomeTests/SomeTests/testImageTextRecognizerBuildsFieldCandidatesForFormLikeLines`，本机失败于 `Unable to find a destination matching ... iPhone 16`，属于当前旧 Xcode/模拟器环境限制；完整 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 98 工作日志自定义导出模板本地验证：新增 `testWorkLogExporterBuildsCustomTemplateDraft` 和 `testWorkLogExporterCustomTemplateKeepsUnknownPlaceholdersVisible`，覆盖自定义模板字段占位符、编号列表、项目汇报优先排序和未知占位符保留。本轮实现后通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/WorkLogSourceFilterEngine.swift SomeTests/SomeTests.swift`、`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Views/ContentView.swift`、`rg -n "customReportDraft|customTemplateDraft|customReportTemplate|defaultCustomReportTemplate|自定义模板|阶段 98" some/Utilities/WorkLogSourceFilterEngine.swift some/Views/ContentView.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md docs/open-source-reuse-audit.md`、`git diff --check`、plist/workflow YAML 校验。完整 SwiftUI typecheck 和 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 99 OCR 分隔符表格候选摘要本地验证：新增 `testImageTextRecognizerBuildsTableCandidateForDelimitedRows` 和 `testImageTextRecognizerSkipsTableCandidateWithoutDataRows`，覆盖分隔符型表格候选摘要和单表头不误判；`rg -n "tableCandidateSummary|tableCells|表格候选|testImageTextRecognizerBuildsTableCandidateForDelimitedRows|testImageTextRecognizerSkipsTableCandidateWithoutDataRows|OCR v10|阶段 99"` 确认实现、测试和文档接线。本轮实现后通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/ImageTextRecognizer.swift SomeTests/SomeTests.swift`、`git diff --check`、plist/workflow YAML 校验。当前本机 Xcode 13.2.1 的 `xcodebuild -showdestinations` 只返回不可用 placeholder，无法直接跑 XCTest；完整 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 100 素材库媒体摘要缓存只读刷新本地验证：新增 `testMediaMetadataCachedSummaryOnlyUsesPreheatedCache`，覆盖媒体摘要缓存预热前不主动读文件、预热后返回图片尺寸摘要；`MediaMetadataExtractor.cachedSummary(for:)` 只读缓存，`AssetRowView.summaryText` 不再同步调用 `MediaMetadataExtractor.summary(for:)`，素材库后台预热完成后用 `mediaCacheVersion` 触发刷新。本轮通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/AttachmentReferenceResolver.swift some/Utilities/VideoThumbnailGenerator.swift some/Utilities/MediaMetadataExtractor.swift some/Views/ContentView.swift SomeTests/SomeTests.swift`、`rg -n "cachedSummary|mediaCacheVersion|testMediaMetadataCachedSummaryOnlyUsesPreheatedCache|视频缩略图与媒体摘要 v3|阶段 100" some/Utilities/MediaMetadataExtractor.swift some/Views/ContentView.swift SomeTests/SomeTests.swift README.md findings.md progress.md task_plan.md verification.md docs/open-source-reuse-audit.md`、`git diff --check`、plist/workflow YAML 校验。当前本机 Xcode 13.2.1 仍无法运行完整 XCTest，完整 XCTest 仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 101 禅定字数目标本地验证：新增 `testZenWritingGoalSummarizesProgress` 和 `testZenWritingGoalClampsProgressFraction`，覆盖目标 raw value、标题、目标字数、进度文案和比例 clamp。先用临时 Swift typecheck 探针确认缺少 `ZenWritingGoal` 时会失败；实现后通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/ZenDraftStats.swift SomeTests/SomeTests.swift`、`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Views/ContentView.swift`、`git diff --check`、plist/workflow YAML 校验。完整 XCTest 和真实专注模式交互仍以 GitHub Actions/Xcode 16 与真机/模拟器验证为准。
- 2026-06-24：阶段 102 图片附件缩略图缓存本地验证：新增 `testImageThumbnailGeneratorCachesDownsampledImage` 和 `testImageThumbnailPreheatFiltersDeduplicatesAndReportsMissingFiles`，覆盖 ImageIO 下采样、缓存文件写入、缓存命中、素材 URL 去重、非图片过滤和缺失文件失败统计；`AttachmentPreviewList` 与素材库 `AssetRowView` 改用 `ImageThumbnailPreview`，素材库维护周期会预热 `ImageThumbnailGenerator.sourceURLs(in:)`。本轮通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Utilities/TagParser.swift some/Utilities/ZenDraftStats.swift some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/AttachmentReferenceResolver.swift some/Utilities/ImageThumbnailGenerator.swift some/Utilities/VideoThumbnailGenerator.swift some/Utilities/MediaMetadataExtractor.swift some/Views/AttachmentPreviewList.swift some/Views/ContentView.swift SomeTests/SomeTests.swift`、`rg -n "ImageThumbnailGenerator|ImageThumbnailPreview|ImageThumbnailCache|testImageThumbnailGeneratorCachesDownsampledImage|媒体缩略图与摘要 v4|阶段 102" ...`、`git diff --check`、plist/workflow YAML 校验。完整 XCTest 与真实长列表滚动仍以 GitHub Actions/Xcode 16 和真机/模拟器验证为准。
- 2026-06-24：阶段 103 手帐列表图片图层缩略图缓存本地验证：新增 `testImageThumbnailPreviewPixelSizeUsesScaledLayerBounds` 覆盖手帐预览缩略图按图层显示尺寸取 3x 且最低 64px；`ScrapbookPagePreview` 图片图层改用 `ImageThumbnailFillPreview`，`rg -n "previewLayer\\(|ImageThumbnailFillPreview|UIImage\\(contentsOfFile: url\\.path\\)" some/Views/ContentView.swift some/Views/AttachmentPreviewList.swift` 确认手帐列表预览不再同步读取原图。本轮通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Utilities/TagParser.swift some/Utilities/ZenDraftStats.swift some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/AttachmentReferenceResolver.swift some/Utilities/ImageThumbnailGenerator.swift some/Utilities/VideoThumbnailGenerator.swift some/Utilities/MediaMetadataExtractor.swift some/Views/AttachmentPreviewList.swift some/Views/ContentView.swift SomeTests/SomeTests.swift` 和 `git diff --check`。尝试 `swiftc -typecheck some/Utilities/ImageThumbnailGenerator.swift` 被当前旧 Xcode/SDK 限制挡在 `no such module 'UIKit'`，完整 XCTest 与真实滚动仍以 GitHub Actions/Xcode 16 和真机/模拟器为准。
- 2026-06-24：阶段 104 手帐编辑器图片图层缩略图预览本地验证：新增 `testImageThumbnailPreviewPixelSizeIncludesLayerScale` 覆盖编辑器按画布缩放和图层缩放计算缩略图尺寸，`ScrapbookEditorView` 图片图层改用 `AsyncScrapbookImageLayerPreview` 异步加载缩略图并后台套滤镜。本轮通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Utilities/TagParser.swift some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/AttachmentReferenceResolver.swift some/Utilities/ImageThumbnailGenerator.swift some/Utilities/ScrapbookRenderer.swift some/Views/ScrapbookEditorView.swift SomeTests/SomeTests.swift`、`rg -n "AsyncScrapbookImageLayerPreview|UIImage\\(contentsOfFile: url\\.path\\)|testImageThumbnailPreviewPixelSizeIncludesLayerScale|阶段 104" ...` 和 `git diff --check`。完整 XCTest、滤镜像素差异和真实拖拽手势仍以 GitHub Actions/Xcode 16 和真机/模拟器为准。
- 2026-06-24：阶段 105 本地 AI 搜索单字符 typo 容错本地验证：新增 `testLocalSemanticSearchMatchesSingleTypoEnglishTerm`，覆盖 `roadmapp` 可命中 `roadmap` 且展示 memo 原词。旧 HEAD 逻辑只取精确共享词，`roadmapp` 与 `roadmap` 没有 shared terms，测试会返回空结果；实现后通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/AI/SemanticSearchEngine.swift SomeTests/SomeTests.swift`、`DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/AI/AIMemoryProfile.swift some/AI/AIInsightComposer.swift some/AI/SemanticSearchEngine.swift some/Utilities/DateFormatters.swift some/Utilities/TagParser.swift some/Utilities/MemoTaskParser.swift some/Utilities/MemoReferenceParser.swift some/Utilities/SharedAttachmentStore.swift some/Models/Memo.swift SomeTests/SomeTests.swift`、`rg -n "localSearchMatches|allowsTypoMatching|editDistanceIsAtMostOne|testLocalSemanticSearchMatchesSingleTypoEnglishTerm|阶段 105" ...` 和 `git diff --check`。尝试把 `OpenAIClient.swift` 纳入旧 Swift parser 会被既有 `guard let text` shorthand 语法挡住；完整 XCTest、OpenAI embedding 路径和 UI 搜索交互仍以 GitHub Actions/Xcode 16 为准。
- 2026-06-24：阶段 106 图片缩略图缓存孤儿清理本地验证：新增 `testImageThumbnailPruneCacheKeepsExpectedFile`，覆盖清理不再引用源图的缓存，同时保留当前源图不同尺寸的缓存；`ImageThumbnailGenerator.pruneCache` 使用源图路径/大小/修改时间前缀保留所有当前尺寸，素材库维护周期在图片缩略图预热后清理 `ImageThumbnailCache` 孤儿 `.jpg`。本轮通过 `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcrun swiftc -parse some/Utilities/MemoTaskParser.swift some/Utilities/Colors.swift some/Utilities/TagParser.swift some/Utilities/SharedMemoStorage.swift some/Utilities/SharedAttachmentStore.swift some/Utilities/AttachmentReferenceResolver.swift some/Utilities/ImageThumbnailGenerator.swift some/Utilities/VideoThumbnailGenerator.swift some/Utilities/MediaMetadataExtractor.swift some/Views/ContentView.swift SomeTests/SomeTests.swift`、`rg -n "testImageThumbnailPruneCacheKeepsExpectedFile|ImageThumbnailGenerator\\.pruneCache|cacheSourceKey|cacheFilename|removedCount|阶段 106" ...` 和 `git diff --check`。完整 XCTest、真实缓存淘汰和长列表性能仍以 GitHub Actions/Xcode 16 和真机/模拟器为准。
- 2026-06-22：手帐画布拖拽、双指缩放和旋转需要完整 Xcode 16 + 模拟器/真机环境做真实手势验证；当前环境已完成模型替换测试、plist/scheme 校验和旧 Swift parser 对可解析文件的检查。
- 2026-06-22：手帐画布编辑需要完整 Xcode 16 + 模拟器/真机验证多指缩放/旋转、拖拽边界、滚动页面内手势冲突和真实图片图层显示；当前环境只能验证模型替换逻辑和静态语法。
- 2026-06-22：图片编辑需要完整 Xcode 16 + 模拟器/真机验证真实照片色彩、保存耗时、素材库 sheet 交互、长边大图内存占用和系统分享/备份链路；当前环境已完成渲染核心静态解析和小图单元测试。
- 2026-06-22：衣橱洞察需要完整 Xcode 16 + 模拟器/真机验证横向统计条、建议填入表单和真实长列表滚动；当前机器只有 Command Line Tools，缺少 iOS/UIKit SDK，无法对包含 UIKit 的 app 文件做完整 `swiftc -typecheck` 或 `xcodebuild test`。
- Share Extension 的真实分享面板、App Group 容器和签名能力需要在完整 Xcode + Apple Developer Team 环境里实机或模拟器验证。
- 旧版 `xcrun swiftc -parse` 只能做语法层检查，不能替代 Xcode 16 的完整 typecheck、XCTest 或 Share Extension 真机验证。
- 旧版 `xcrun swiftc -parse` 会继续解析 AppIntents 源文件中的 Swift concurrency / AppIntents 语法，不能用来证明 `CI_DISABLE_APP_INTENTS` 下的 Xcode 16 条件编译结果；CI 复验以 GitHub Actions 为准。

## 下一步建议

在安装完整 Xcode 的 Mac 上打开 `some.xcodeproj`，设置 Team 和 Bundle ID 后运行一次模拟器，再执行 Product > Archive。
